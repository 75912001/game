# 基本要求
    深度思考 
    分析背景
    明确问题
    评审当前想法
    查阅可参考资料
    完成和任务
    给出详细的解决方案.
    注重逻辑清晰和步骤明确.
    不要直接给出代码实现, 而是给出如何实现的思路.
    注重性能和可维护性.
    如若有更好的方案, 请一并给出.并给出理由和对比.
    可以完全访问当前项目的代码和资源.
# 背景
    现在在场景中增加一个 tile , 作为大型建筑. 
    方向从屏幕上方中点,向右侧中点延伸. 矩形.
        按照当前的角色和场景的绘制顺序(Y轴排序).
# 问题
    角色走到建筑左下方(角色wy < 建筑的 wy)会被建筑遮挡(这里是建筑前方, 应该是角色遮挡建筑).
# 当前想法
    当前想法:
        想通过代码实现自动垂直拆分(数值越小,拆分的片数越多, 越精细)建筑 tile 为 多片.
            每一片 tile 分别绘制.
            每一片的Y最大值,应该是该片最底部且具有像素单位的点.
            将每一片放入新的 tile 缓存中.
# 可参考资料
    当前已添加属性:
        const TiledLayerProperty_VerticalSlicePixel = "verticalSlicePixel" // 图块层-按照像素值-垂直切分
        verticalSlicePixel 是建筑物垂直切分的像素值.
    建筑信息位置: C:\game\sa\client\res\building\7000002.png
    地图信息位置: C:\game\sa\client\res\tiled\map
# 任务
    解决上述问题
    加载配置的时候进行拆分.
    绘制的时候, 按照拆分后的片进行绘制.
    保持现有的绘制顺序(Y轴排序)不变.
    保持现有的代码结构和逻辑不变.